# https://taskfile.dev

version: '3'

vars:
  GREETING: Welcome to the CMS task file

tasks:
  default:
    cmds:
      - echo "{{.GREETING}}"
    silent: true

  dev:
    cmds:
      - cd development; air
